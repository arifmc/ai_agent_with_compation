Ai agent for searching the web, extracting web, and crawling web using tavily

# agent

Generated by `prisma orm init` with the Minimal template.

## First run

1. `pnpm run db:init` — applies your contract to the database (creates tables, signs the marker).
2. `pnpm run dev` — runs `src/index.ts`, which writes one row and reads it back.

## Available scripts

- `pnpm run dev` — run the Prisma ORM sample script

### Database and migrations

- `pnpm run contract:emit` — emit contract artifacts after contract changes
- `pnpm run db:init` — initialize database state manually
- `pnpm run db:update` — update database state manually
- `pnpm run migration:plan` — create a migration plan
- `pnpm run migrate` — apply a planned migration
- `pnpm run db:seed` — insert sample users manually (run after `db:init`)

## Prisma ORM

Prisma ORM setup is scaffolded in:

- `prisma/contract.prisma`
- `prisma.config.ts`
- `prisma/db.ts`
- `src/lib/prisma.ts`

For provider-specific Prisma ORM reference docs, see `prisma-8.md`. Prisma ORM skills live in the upstream `skills/` directory: https://github.com/prisma/prisma/tree/main/skills.

Node-based Prisma ORM projects expect Node.js 24 LTS or newer.
